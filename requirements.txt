# Gideon — local automated ML pipeline
# Python 3.10+

# Core data stack
pandas>=2.0
numpy>=1.24
pyarrow>=14.0          # fast, typed Parquet artifacts exchanged between agents

# Modeling
scikit-learn>=1.3
xgboost>=2.0
joblib>=1.3           # model (de)serialization

# Orchestration / triggering
watchdog>=3.0         # inbox/ file watcher

# Dashboard
streamlit>=1.30
plotly>=5.18          # interactive charts in the dashboard
streamlit-autorefresh>=1.0  # state-preserving auto-refresh (websocket rerun)

# logging 
psutil>=5.9
